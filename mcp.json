{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "nomina": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "--frozen", "--no-dev", "server.py"]
    }
  }
}
